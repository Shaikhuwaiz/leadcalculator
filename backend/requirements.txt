Django>=6.0.4
django-ninja
gspread
oauth2client
gunicorn
django-cors-headers
redis
python-dotenv

# Optional: RAPIDS cuDF for GPU-accelerated sheet processing (Linux + NVIDIA GPU only).
# pip install cudf-cu12 --extra-index-url=https://pypi.nvidia.com
# cudf-cu12